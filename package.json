{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "yarn lerna run start",
    "build": "yarn lerna run build"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^7.1.3",
    "prettier": "^3.0.0"
  }
}
